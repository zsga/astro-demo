---
import Header from '@/components/Header.astro';
import BaseLayout from './BaseLayout.astro';

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<BaseLayout {title}>
  <Header />
  <div class="bg-base fg-base max-w-3xl mx-auto px-6">
  <slot />
  </div>
</BaseLayout>
